# CLAUDE.md — working on r-docs (repo dir: r-docs)

A Next.js (App Router) + Prisma/SQLite + TipTap app that runs either the Claude Agent SDK or OpenAI Codex SDK for document edits, comment replies, and repo work. See `README.md` for the product description.

## Running the app

The service is deployed blue/green via `./deploy/deploy.sh` (see `deploy/README.md`): Caddy on `:14141` load-balances to app instances on `:14142`/`:14143`. `./manage.sh <start|stop|restart|status|logs>` wraps this for day-to-day operation, and `../manager.sh r-docs <command>` delegates to it — `restart` runs a zero-downtime deploy, never a kill-and-relaunch.

You are allowed to manage the service yourself when debugging — deploy whenever a server-side change needs to be live:

```bash
# Zero-downtime deploy of the current working tree (also bootstraps the
# whole stack if nothing is running). Build takes a few minutes; run it in
# the background (Bash run_in_background) and watch its output.
./deploy/deploy.sh   # or: ./manage.sh restart

# Verify:
./manage.sh status
curl -s localhost:14141/api/health
```

Never free port `14141` by killing its listener — that is the **load balancer**, not the app; killing it takes the site down while both app processes keep running unreachable behind it (this happened for real via the old manager.sh restart). Do not start the dev server (`npm run dev`); production `npm run start` is what the deploy uses.

### How the blue/green deploy works

`./deploy/deploy.sh` is the blue/green deploy path — see `deploy/README.md`. Caddy on `:14141` proxies to app instances on `:14142`/`:14143`; each release builds into its own dist dir (`NEXT_DIST_DIR=.next-blue|green`), and after the upstream switch the old process **drains gracefully**: Slack socket + scheduler stop, in-flight agent runs finish where they started (shared DB + Slack Web API), then it exits. `GET /api/health` reports `{ok, draining, activeRuns, pid, distDir}` (503 while draining); `POST /api/admin/drain` (Bearer `DEPLOY_SECRET` from `.env`) starts a drain. The boot sweep in `instrumentation.ts` is **silence-based** (`sweepAbandonedAiRuns`) — never revert it to "fail every RUNNING run at boot", or a new process kills the draining sibling's runs. Migrations must be expand/contract (old code serves the new schema during overlap). `legacy-single-process.sh` stays as the legacy/emergency single-process path; never run both at once — stop the full stack first (`./manage.sh stop`) before falling back.

- Public URL: `https://docs.nielsrolf.com`
- Local: `http://localhost:14141`
- The app sits **behind Cloudflare**, which terminates origin connections at ~100s (HTTP 524). Anything synchronous that takes longer than that will fail client-side even if the origin completes.

## Where logs live

Service stdout/stderr is captured in `logs/service_YYYYMMDD_HHMMSS.log`. There is one file per restart. The most recent one is what you want — `ls -lt logs/ | head -3`.

Two log streams converge here:

1. **Server-side `console.log` / `console.warn` / `console.error`** from API routes and `lib/`. Search-friendly prefixes:
   - `[ai-edit] finished {...}` — every AI selection edit run. JSON has `aiRunId`, `documentId`, lengths, `replacementIsEmpty`, `replacementEqualsSelection`, `fallbackFired`, image/widget counts, commit info, model.
   - `[ai-edit] suspect {...}` — same payload, logged at warn level when the agent submitted empty or unchanged replacement text.
   - `[client-log:<level>] {...}` — every event the browser POSTs to `/api/client-log`. Includes `scope`, `userId`, `message`, `data`.

2. **Client-side events**. The browser does not write the service log directly — it POSTs to `/api/client-log`, which `console`s the payload with the `[client-log:…]` prefix. Helper: `logClientEvent({ scope, level, message, data })` in `components/document-workspace/utils.ts`. Every `setGlobalError` in `components/document-workspace.tsx` goes through `reportClientError(message, scope, data?)`, which both shows the toast and logs.

### Useful grep patterns

```bash
# Latest log file
LOG=$(ls -t logs/service_*.log | head -1)

# All AI edit summaries
grep "\[ai-edit\]" "$LOG"

# All client errors
grep "\[client-log:error\]" "$LOG"

# Everything about one document
grep "<documentId>" "$LOG"

# Everything about one AI run
grep "<aiRunId>" "$LOG"
```

`aiRunId` is the strongest correlation key — it threads through the server `[ai-edit]` line, the client `[client-log]` payloads, and the `AiRun` / `AiRunEvent` / `DocumentVersion` tables.

## Debugging: which surface failed?

When something looks broken, ask "did the server finish?" before "did the agent get it right?". The three layers each fail differently:

| Symptom | Where to look |
| --- | --- |
| User sees toast "AI edit failed." with `status:524` and `elapsedMs ≈ 100-130s` | Cloudflare timeout. Check the same `[ai-edit] finished` line later in the log — the agent very likely succeeded but the response was killed in flight. |
| `[client-log:error] scope:ai-edit-marker-lost` | The `aiEditRange` mark covering the selection was wiped before the agent finished (collab step, remote update, doc reset). The payload includes `presence` (plugin state vs. mark state) — that tells you whether the mark or the plugin entry was lost. |
| `[client-log:warn] scope:ai-edit-apply` with `applied:false` or `charDelta:0` | Editor silently rejected the inserted content. Check schema compatibility of what `buildAiEditInsertContent` produced. |
| `[client-log:error] scope:ai-edit-apply-threw` | Exception during `insertContentAt` / `saveDocument` — payload includes stack. |
| `[client-log:error] scope:ai-edit-save-failed` | Editor changed locally but PATCH `/api/documents/:id` did not persist. Compare client docSize vs. server `Document.content`. |
| `[client-log:error] scope:save-document` | Any background autosave failed. |
| `[client-log:error] scope:comment-anchor` | `commentAnchor` mark could not anchor — payload reports `selectedNodeType`, `nodesInRange`. Block-node anchors are stored as `commentThreadIds` attrs on `embeddedWidget` / `repoImage` / `image`; text anchors via the `commentAnchor` inline mark. |
| `[ai-edit] suspect` with `replacementIsEmpty` or `replacementEqualsSelection` | Agent output was a no-op. The `validateSubmission` guard in `app/api/documents/[id]/ai-edit/route.ts` should have caught it and asked the agent to retry — if it didn't, the guard needs widening. |

If the log alone can't answer a question, the database can. Don't query the SQLite file directly — use a one-off Prisma script. Prior tasks have used the pattern below; clean it up when done:

```ts
// scripts/_inspect.ts (gitignored)
import { db } from "../lib/db";

async function main() {
  const run = await db.aiRun.findUnique({
    where: { id: "<aiRunId>" },
    select: { status: true, error: true, startedAt: true, finishedAt: true, instruction: true }
  });
  const events = await db.aiRunEvent.findMany({
    where: { aiRunId: "<aiRunId>" },
    orderBy: { createdAt: "asc" },
    select: { role: true, message: true, createdAt: true }
  });
  const versions = await db.documentVersion.findMany({
    where: { documentId: "<documentId>" },
    orderBy: { createdAt: "desc" },
    take: 5,
    select: { id: true, createdAt: true, aiRunId: true, content: true }
  });
  console.log({ run, events, versions });
}

main().finally(() => db.$disconnect());
```

Run with `set -a && . ./.env && set +a && npx tsx scripts/_inspect.ts`. The DB is SQLite at the path in `.env`'s `DATABASE_URL`.

## How the AI edit pipeline fits together

User flow, for context when reading logs:

1. User selects text → `handleAiEdit` in `components/document-workspace.tsx`.
2. Client marks the selection with an `aiEditRange` mark + a plugin-state entry (`upsertAiEditSelection`), keyed by `selectionId`.
3. `POST /api/documents/:id/ai-edit` (`app/api/documents/[id]/ai-edit/route.ts`):
   - Creates the `AiRun` row.
   - Ensures an isolated git worktree under `.research-workspaces/<documentId>/worktrees/...`.
   - Runs the document-selected harness through `AgentRunner`: `runClaudeResearchAgent` for Claude Code or `runCodexResearchAgent` for Codex.
   - Claude finishes through the `submit_response` MCP tool; Codex returns the equivalent strict structured response. Both pass through `validateSubmission`.
   - Logs `[ai-edit] finished {...}` regardless of outcome.
   - Returns `replacementText`, `images`, `widgets`, `aiRunId`, etc.
4. Client locates the marker again (`getAiEditSelectionRange(selectionId)` — falls back from plugin state to mark scan), `buildAiEditInsertContent` parses replacement Markdown into TipTap nodes, `insertContentAt` applies it, `saveDocument` PATCHes the new doc back.

Every step is now logged. If you change any of them, keep the `scope` strings stable so historical greps still work.

## MCP bridge (external agents editing documents)

`POST /api/mcp` is a stateless streamable-HTTP MCP server (hand-rolled JSON-RPC in `lib/mcp/server.ts` — initialize / tools/list / tools/call only). Users connect a local Claude Code with the one-liner from the **AI settings page (topbar) → Connect via MCP** button, which mints an `ApiToken` (SHA-256-hashed, `lib/api-tokens.ts`) and copies:

```
claude mcp add --transport http r-docs <APP_URL>/api/mcp --header "Authorization: Bearer gdai_…"
```

## Document agent API channels

External products trigger configured r-docs agents through a generic, document-scoped
channel (`AgentApiChannel`, `lib/agent-api-channels.ts`), never through a user's broad
MCP token. `POST /api/documents/:id/api-channel` rotates the one-time plaintext
`gdach_...` token. The integration calls `POST /api/agent-channels/:triggerId/runs`
and polls `GET .../runs/:runId`; it can trigger and inspect only that document.
`POST /api/agent-setups` is the signed-in one-click provisioning seam: it creates a
document with initial Markdown, environment and one skill, then returns its scoped
channel. `/agent-setup` is the current browser flow used by forecasting; service-specific
prompts and callbacks belong in the integrating service, not in the channel runner.
Manifest fetches and callbacks are server-side (so HTTPS can integrate with a
tailnet-only HTTP service) and restricted to `AGENT_SETUP_ALLOWED_ORIGINS`.
`GET /authorize?redirect_uri&state` + `POST /api/authorize` (`lib/integration-signin.ts`) is
the generic **"Sign in with r-docs"** seam: a signed-in user consents, and r-docs redirects
back with a 2-minute HS256 id token (`sub`, `email`, `name`, `aud` = redirect origin) signed
with `INTEGRATION_SIGNIN_SECRET`. Redirect origins reuse `AGENT_SETUP_ALLOWED_ORIGINS`. The
integrating service verifies the token and keeps its own session; r-docs stores nothing.

Key invariants:

- **All content edits go through the collab step pipeline** (`lib/mcp/apply-edit.ts` → `submitCollaborationSteps`), never a direct content write. Live clients see MCP edits over SSE; stale-version pushes retry up to 3×.
- **Markdown → nodes uses the same pipeline as the browser**: `buildAiEditInsertContent` → markdown-it → `generateJSON` (`@tiptap/html`, in `lib/mcp/markdown-doc.ts`), so `![widget: label](widget://<id>)` placeholders and repo-relative image paths behave identically to built-in agent edits.
- **`upload_files` / `create_widget` write into the document's base workspace** (git-committed under `withWorkspaceLock`, path-traversal-guarded in `lib/mcp/workspace-files.ts`), so `/widgets/<id>/source` and `/repo-files` can serve the artifacts. `create_widget` follows the same host-build policy as the manual `POST /widgets` route.
- **Forum surfaces are first-class over MCP**: a quicktake is a `Document` with `kind:"quicktake"` whose text lives in `quicktakeBody` (its TipTap `content` is empty), so `read_document` returns that body as `markdown` and the markdown WRITE tools refuse quicktakes via `requireEditableDocument` (silently rewriting the empty content was the old failure). `list_quicktakes` exposes the viewer's readable feed (own + public + group-shared) with full bodies, and `list_documents` includes `DocumentGroupAccess` grants (share-with-my-team has no membership row) plus `kind`/`posted_to_forum`. Test: `tests/mcp-forum-access.test.ts`.
- Tool schemas are zod (`lib/mcp/tools.ts`), converted with `z.toJSONSchema` for `tools/list`. Tool-level failures (bad `find_text`, missing access) return `isError: true` results so the model can self-correct; only malformed JSON-RPC gets protocol errors.
- Tests: `tests/mcp-server.test.ts` (headless, real SQLite + collab pipeline).

## Bug-fix workflow (required)

When the user reports a bug, **reproduce it first with a new test case that initially fails, then fix it.** Concretely:

1. Write a test that exercises the reported flow and asserts the *correct* behavior. Run it and confirm it **fails** for the reason the user described (a failing test that fails for the wrong reason proves nothing).
2. Only then make the code change.
3. Re-run the test and confirm it now **passes**, and that the rest of the suite stays green.
4. Keep the test — it is the regression guard.

Prefer the cheapest layer that genuinely reproduces the bug: a headless test in `tests/` (real ProseMirror + real SQLite, no browser/LLM) when possible, or the HTTP integration suite (`tests/integration/`, real routes + auth) when the bug needs the request path. See the testable seams already extracted for collab mapping, AI-edit guards, anchor tracking, and widget paths.

## Conventions

- **Don't bypass the agent submission validator.** If the agent regularly trips a check, prefer to fix the prompt or widen the validator (`validateSubmission` in `route.ts`) rather than removing the guard.
- **Don't add new `setGlobalError` calls** in `components/document-workspace.tsx`. Use `reportClientError(message, scope, data?)` so the toast and the log stay in sync.
- **Don't add new `console.log`s on the server without a `[scope]` prefix** — the `logs/` files are searched by prefix.
- **Credential failures are classified by code, never by message** (`lib/user-credentials.ts`): `resolveModelCredentialEnv` throws an `AgentCredentialError` carrying `{ code, provider, envKey }` (`"anthropic-credential-missing"` | `"provider-key-missing"`), and the fallback branches (`anthropicRunUsesFreeFallback`, the free-local and Codex→LiteLLM re-routes in `loadAgentEnvWithFreeFallback`) branch with `isAgentCredentialError(error, code)`. Message text is for humans only — do not reintroduce `error.message === CONNECT_CREDENTIAL_MESSAGE` or `error.message.includes("OPENAI_API_KEY")`, which made a reworded string silently disable a fallback and let any unrelated error mentioning a key name hijack the LiteLLM re-route. `credentialRequirementError` / `providerKeyRequirementError` remain as string wrappers over `*Failure` for UI copy. Test: `tests/local-model.test.ts`.
- **The `AI agent` running edits writes inside `.research-workspaces/<documentId>/worktrees/...`**, never in the r-docs repo itself. Despite the historical directory/function names, managed runs use a self-contained no-local clone there (a real `.git/` directory), not `git worktree add`: the container mounts only that directory, so a gitfile pointing into the unmounted base clone would break every git command. Every run starts from the app-managed base workspace `HEAD` (including its snapshot of uncommitted changes), fetches the configured remote, then merges the freshly fetched explicit branch or `origin/HEAD`; therefore it sees both local/unpushed workspace state and the newest non-conflicting remote state. A true conflict is resolved only in the isolated run checkout with the local workspace version preferred. Claude and Codex receive the exact same prepared host path mounted at `/workspace`; harness selection happens afterward and must never change workspace content. Completed commits are fetched back into the base object database before merge. Keep the one-workspace-mount isolation invariant. `runClaudeResearchAgentOnce` will throw if no isolated workspace is supplied. Regressions: `tests/workspace-link.test.ts`, `tests/agent-container.test.ts`.
- **Schema changes** must be mirrored on both the client TipTap schema (`components/document-workspace/nodes.tsx` + `document-workspace.tsx`) and the server schema (`lib/document-editor-schema.ts` + `lib/document-schema-nodes.ts`). The server schema is used to parse and re-render document content out-of-browser.
- **Tests**: `npm test` runs the headless suite (`tsx --test tests/*.test.ts`); `npm run test:integration` runs the HTTP suite against a running server (`GDOCS_TEST_URL`, default `http://localhost:14141`, skips if unreachable); `npm run test:e2e` runs Playwright. Lint with `npx tsc --noEmit -p .` and `npx next lint`.
- **Models & providers** (`agent-core/agent-config.ts`): `Document.agentModel` stores a canonical Anthropic id (`claude-sonnet-5` default, `claude-fable-5`, `claude-opus-5`; legacy rows may still hold the aliases `sonnet`/`opus` or the superseded `claude-opus-4-8`, all normalized on read), an OpenRouter model as `openrouter/<author>/<model>` (curated list + custom slug in the UI, gated on the document env having `OPENROUTER_API_KEY`), or a LiteLLM model as `litellm/<model-name>` (gated on `LITELLM_API_KEY`; also needs `LITELLM_BASE_URL` — per-doc, or the server-wide default in `.env`, which on this deployment is `http://host.docker.internal:9274`, the host's ssh tunnel to litellm.nielsrolf.com). Both third-party providers use the same Claude Agent SDK pointed at an Anthropic-compatible endpoint: `applyProviderEnv` (`agent-core/agent-env.ts`) swaps in `ANTHROPIC_BASE_URL`/`ANTHROPIC_AUTH_TOKEN` and strips host Anthropic credentials; it throws if the key is missing (never silently bills the host account), and the container runner skips host-OAuth injection for provider-key jobs (`resolveContainerCredentialEnv`). Provider routing is `agentModelProvider(model)`. `AiRun.model` labels: `claude-agent-sdk:<id>[+effort]` vs `openrouter:<slug>` vs `litellm:<name>` vs `local:<name>`. Storable-value validation is shared client/server via `isStorableAgentModel`. Additionally, connected provider keys are injected as tool credentials regardless of the selected model (`applyToolCredentialEnv` in `lib/user-credentials.ts`; document env wins; Anthropic stays model-gated because `ANTHROPIC_*` is the SDK routing channel): `OPENAI_API_KEY`, `OPENROUTER_API_KEY`, and `LITELLM_API_KEY` resolve from the triggering user and then the document owner; personal Hugging Face credentials become `HF_TOKEN` and resolve from the actual triggering user only, never the owner fallback (including self-hosted documents). With the broker on, the LLM tool keys are brokered whenever present, not just for the matching model provider; `HF_TOKEN` is not brokered because Hugging Face clients do not have a single reliably overridable upstream. Users also carry a personal default (`User.defaultAgentModel/-Effort`, set on `/slack/connected` after Slack linking, API `GET/PATCH /api/user/agent-defaults`); ALL agent runs (Slack, doc conversation, selection edit, comment reply) resolve doc config → triggering user default → app default (`resolveAgentConfigForUser` in `lib/agent-defaults.ts`, guarded by `tests/doc-user-default-model.test.ts`), and the doc page seeds the agent panel with the same effective config for the viewer. A fourth provider `local/<name>` targets the deployment's own llama.cpp server (Anthropic-compatible `/v1/messages`, tool use verified on qwen3.6-27b): no credential at all, just `LOCAL_MODEL_BASE_URL` + `LOCAL_MODEL_NAME` in `.env` (currently `http://host.docker.internal:8080` because llama-server binds 127.0.0.1 on this host; if it moves to another tailnet machine, start it with `--host 0.0.0.0` and put that machine's tailnet IP here). It is also the **automatic free fallback**: an Anthropic-model run with no credential anywhere runs on the local model instead of failing (`loadAgentEnvWithFreeFallback` in `lib/user-credentials.ts`, used by all three agent routes; the run timeline records the substitution).
- **No host credentials, ever.** Agent/model/GitHub credentials resolve only from document env or encrypted `UserCredential` rows (triggering user, then document owner). Host environment keys, `~/.claude`, and `~/.codex/auth.json` are never injected, copied, mounted, or used as fallbacks. Future ChatGPT-subscription support must store and refresh the login in the user's account and materialize it only inside that user's isolated run credential area. Enforcing this needs more than not *injecting* the credential: `HOME` is allowlisted, so `applyAgentConfigDirEnv` (`agent-core/agent-env.ts`) pins `CLAUDE_CONFIG_DIR` / `CODEX_HOME` to a run-scoped dir on EVERY path (in-process, container entrypoint, self-hosted worker, both merge resolvers) — otherwise the CLI finds the host's `~/.claude` session and *retries a rejected request with it*. With the broker on that surfaces as a run-killing `401 Credential broker: Missing or malformed broker token.`; without it, the host account silently pays. Runners pass the per-conversation session dir (which is also where transcripts land, so in-process runs get real resume); with none, agent-core falls back to a temp dir, never `$HOME`. Test: `tests/agent-config-dir.test.ts` drives the real CLI against a fake 401 endpoint with a planted host session.
- **GitHub auth is per-document, never global** (`resolveGithubAuthForDocument` in `lib/user-credentials.ts`): doc env `GITHUB_TOKEN` → triggering user's GitHub PAT (`UserCredential` provider "github") → owner's PAT. `ensureLinkedRepository` pins the resolved token as a repo-local `http.https://github.com/.extraheader`; no token → anonymous git. The host `GITHUB_TOKEN` is never considered.
- **Shared workspaces** (`Document.workspaceDocumentId`, `lib/workspace-link.ts`): a document can share another document's base workspace instead of linking a repo (mutually exclusive with `repoUrl` — setting either clears the other). Two directions: doc → `slack_channel` (the doc joins the channel's workspace) and `slack_channel` → doc (the channel joins a regular doc's workspace — "start with a doc, then create the channel"; the target doc must own its workspace, i.e. have no link itself). Link-time rules keep chains at most doc → channel → doc and cycle-free; resolution happens in ONE place, `resolveWorkspaceDocumentId` in `lib/research-workspace.ts` (up to TWO hops with a visited-set cycle guard; a dangling id falls back to the last resolvable doc), so ai-edit, conversations, ask-ai, repo-files, widgets, and MCP all inherit it via `ensureLinkedRepository`. Worktrees live under the workspace-owning doc's dir (`LinkedRepository.workspaceDocumentId`), and GitHub auth resolves against that doc. Linking requires EDIT access on the target document (API `GET/PATCH /api/documents/[id]/workspace-link`, UI in the Repo menu; from Slack, `set_channel_workspace` links the channel to a doc and `set_channel_repository` links its backing doc to a GitHub/HuggingFace repo; both accept `"none"` to disconnect). A channel linked to a doc always reads it: `getLinkedWorkspaceDocContext` appends the doc's plain text to the conversation context in `lib/agent-conversation.ts` (anchoring surfaces stay on the channel doc's own text). Tests: `tests/workspace-link.test.ts`, `tests/slack-agent-tools.test.ts`.
- **Skill catalog** (`lib/skill-catalog.ts`): a curated public git repo of skill folders (default `longtermrisk/claude-skills`, override `SKILL_CATALOG_GIT_URL` — accepts local paths, which is how `tests/skill-catalog.test.ts` works). Shallow-cloned into `.skill-catalog/` (10 min TTL, atomic rename, stale-on-failure). `GET /api/skill-catalog` lists; `{ catalogName }` on `POST /api/user/skills` or `POST /api/documents/[id]/skills` installs with one click (UI: document Skills menu + AI-credentials skills section). Installs reuse `prepareSkillUpload`, so the same sanitization/limits apply. The agent system prompt also discloses run env var NAMES (doc env + injected creds + LiteLLM/local host config; never values) via `agentEnvKeysForPrompt` in `agent-core/agent-env.ts` — an agent-core change, so image rebuilds apply.
- **Conversation session resume** (`lib/agent-sessions.ts`): follow-up conversation runs resume the selected harness's native session. Claude owns `$CLAUDE_CONFIG_DIR/projects/**/<sessionId>.jsonl`; Codex owns `$CODEX_HOME/sessions/**/rollout-…<threadId>.jsonl`. Each conversation gets a mounted native config root. The DB stores only the opaque SDK thread/session id in `AiRun.sdkSessionId`; it never serializes a Codex rollout into app schema or reconstructs one from `AiRunEvent`. Auth is separate from rollout persistence and never comes from the host. A recorded session whose transcript is gone (GC'd/deleted) still degrades to the clipped `buildConversationHistory` replay, but NEVER silently: `planSessionResume` returns `resumeUnavailableSessionId` and the conversation run records a visible timeline event saying earlier tool calls are not in context.
- **Context compaction** (`DEFAULT_AUTO_COMPACT_WINDOW` in `agent-core/agent-env.ts`): Claude Code's auto-compaction is on by default, but its default window is the model's FULL window (200k), so compaction only triggers at ~167k and one fat tool result can blow past the hard limit first → `Prompt is too long`, and a session that ends over the limit can never be resumed. `buildAgentEnv` therefore injects `CLAUDE_CODE_AUTO_COMPACT_WINDOW=150000` (host `.env` or document env override it). The CLI **clamps this to the model window**, so a value ≥ the window is a silent no-op — which is why the window and the `context-1m-2025-08-07` beta are decided together in `applyLongContextEnv`, called from `agent-core/agent.ts` right after `applyProviderEnv`: when the beta will really take effect it passes `Options.betas` and raises the window to `LONG_CONTEXT_AUTO_COMPACT_WINDOW=500000`; otherwise it passes no betas and leaves 150k. `usesLongContext` gates that on **provider `anthropic` + API-key auth**, and both halves are mandatory: third-party Anthropic-compatible endpoints (OpenRouter/LiteLLM/local) do not widen a window for this beta, and the CLI *discards* caller-supplied betas under subscription/OAuth auth ("Custom betas are only available for API key users") — declaring 500k in either case clamps back to 200k and switches compaction off entirely, i.e. reintroduces the original bug. A non-entitled API key is safe on its own: the CLI catches the 1M-credits rejection and clamps itself back to 200k. An explicit `.env`/document window always wins over both defaults. Floor 100k, ceiling 1M. Note the container env file carries only the 150k baseline; the upgrade happens in-container, where the run's credential shape is known. Tests: `tests/agent-env.test.ts`.
- **Credential broker** (`lib/credential-broker/`): with `AGENT_CREDENTIAL_BROKER=1`, resolved account/document LLM credentials become per-run virtual keys and the broker swaps virtual→real only while the run remains active. It never resolves host credentials. Request bodies are buffered once at this trust boundary so OpenAI-compatible Responses endpoints receive a concrete `Content-Length`. Covered: Anthropic API/OAuth, OpenAI, OpenRouter, and LiteLLM credentials.
- **Agent-panel run list** (`lib/ai-runs.ts`): the document poll returns up to `AI_RUN_LIST_LIMIT` (200) runs, but only the newest `AI_RUN_EVENT_RUNS` (12) carry event timelines inline; older runs come back with `eventsOmitted: true` and the client lazy-loads their events from `GET /api/documents/[id]/ai-runs/[runId]` (which now includes `events`) when the conversation is opened (`archivedRunEvents` state in `document-workspace.tsx`). Don't put events back on all rows — that bloats the 2s poll. Test: `tests/ai-run-list-window.test.ts`.
- **Agent timeline + result rendering** (`components/document-workspace/agent-timeline.tsx`, `agent-panel.tsx`, `run-result.ts`): tool events arrive as `"<ToolName>: <json>"` strings; the client parses them and renders custom rows (Bash terminal, Edit/MultiEdit/Write diffs, TodoWrite checklist, `mcp__server__tool` prettified to `server: tool name`). The lifecycle system messages emitted by `agent-core/agent.ts` ("Starting Claude research agent." / "Submitting final response." / "Preparing document update.") are matched VERBATIM by `lifecycleStepLabel` to render as quiet step rows, and the final-reply badge keys off the submit step — rewording those strings in agent-core requires updating the client matcher (and vice versa). Structured application outputs are not reconstructed from this lossy event stream: run detail returns the persisted final edit, comments (including the actual triggering-thread reply), and suggestions; `RunResultBlock` renders them for every turn and suppresses a final-reply duplicate when a newer event already contains the exact body. `toolInputSummary` deliberately ships clipped `old_string`/`new_string`/`content` so diffs can render; keep per-field clips small enough that the JSON stays parseable under the 1400-char event cap (unparseable JSON degrades gracefully to a raw-text row). The RESULT side is the richer source and works for historical runs: `parseToolResultData` interprets tool_use_result payloads (Read → line-numbered file view, Edit → diff from `oldString`/`newString`, Bash → `stdout`/`stderr` terminal, Write → added-file view, Grep matches), and tool results also arrive in a second, **content-block** shape — `[{"tool_use_id":…,"type":"tool_result","content":"…"}]` — which `unwrapToolResultText` flattens before per-tool mapping (Read strips the `NNN\t` gutter and recovers `startLine`); without that unwrapping those results render as raw JSON, and `extractJsonStringField` recovers string fields from payloads clipped mid-JSON by the event cap. Tests: `tests/agent-timeline.test.ts`, `tests/agent-run-result.test.ts`.
- **Session plan rail** (`components/document-workspace/todo-outline.ts`, `agent-todo-outline.tsx`): every `TodoWrite` snapshot of a conversation is folded into ONE ordered outline rendered as a right-hand column of `.agent-main` — status as of the newest snapshot, the current step highlighted, click scrolls the timeline to the anchor event (`data-agent-event-id` on tool blocks). Two todo shapes must keep working: Claude's `{content,status}` and the Codex SDK's plan items `{text,completed}` (`normalizeTodoItem`); a payload clipped by the 1400-char event cap is recovered by a positional regex scan, and `toolInputSummary` clips per todo so the JSON usually stays parseable. Newer Claude Code builds ship the **Task\* family** (`TaskCreate`/`TaskUpdate`/`TaskList`/`TaskGet`) INSTEAD of `TodoWrite`, and it is incremental, not snapshot-based: a task's id appears only in the `tool_result` text of its create call (`Task #3 created successfully: …`), so `buildTodoOutline` walks `tool` + `tool_result` events together, binds `task:<id>` keys, applies `TaskUpdate` status changes, and drops `status:"deleted"` items. Both families are in `CLAUDE_AGENT_TOOLS` (`agent-core/ai-tools.ts`) — without them Claude runs emit no plan at all. Test: `tests/agent-todo-outline.test.ts`.
- **Harness routing / Codex**: `codex/openai/<model>` uses native OpenAI Responses auth from a connected/document `OPENAI_API_KEY`; `codex/litellm/<model>` uses LiteLLM's OpenAI-compatible Responses endpoint (`LITELLM_API_KEY` + `LITELLM_BASE_URL`). When a native Codex selection has no OpenAI credential but LiteLLM is connected, `loadAgentEnvWithFreeFallback` transparently routes the equivalent `codex/litellm/openai/<model>` and records the substitution in the run timeline. The configurator chooses LiteLLM by default when it is the only available Codex provider (`defaultCodexAgentModelForCredentials`: OpenAI key → ChatGPT subscription → LiteLLM). Direct `codex/anthropic/*` values are invalid. For Slack-triggered runs, both harnesses expose the same run-scoped Slack capabilities through `/api/slack/agent-tools`: Claude uses the legacy in-process tool callbacks and Codex mounts that endpoint as the `gdocs` MCP server, alongside the document `rdocs` MCP server. The shared tool set includes posting to the current thread, listing/reading channels and threads, recent activity, scheduling, and file sending; membership checks remain server-side. Regressions: `tests/slack-agent-tools.test.ts`, `tests/slack-agent-tools-mcp.test.ts`, `tests/codex-agent.test.ts`.
- **Codex ChatGPT-subscription auth** (`codex/chatgpt/<model>`, provider `openai-chatgpt`): the credential is the user's whole `~/.codex/auth.json` (OAuth tokens bundle) stored as a `UserCredential` (provider `"openai-chatgpt"`, pasted under Settings — `looksLikeCodexAuthJson` auto-detects it, `normalizeCodexAuthJson` validates + compacts) or as document env `CODEX_CHATGPT_AUTH_JSON`. Account-managed, never host-managed: the host `~/.codex/auth.json` is never read. The env var is TRANSPORT ONLY — `seedCodexChatgptAuth` (`agent-core/codex-agent.ts`) materializes it into the run-scoped `$CODEX_HOME/auth.json` (mode 0600) before the app-server starts, then deletes both the blob and `OPENAI_API_KEY` from the child env (never mix API-key auth into a subscription run; `preferred_auth_method:"chatgpt"`). The Codex CLI ROTATES the refresh token on refresh, so after every run `collectRefreshedCodexAuth` reads the file back and a changed, well-formed blob flows through `options.onCodexAuthRefreshed` → the `"codex_auth"` session frame (all three transports: in-process, piped container, detached session) → `persistRefreshedCodexAuth` (`lib/user-credentials.ts`), which updates the stored credential last-writer-wins on `last_refresh` and deliberately never writes back doc-env-sourced blobs or older rotations — never re-seed a later run from a stale original. Never log, commit, or persist the blob as an `AiRunEvent`. Known MVP limits: a crash between rotation and persist can lose the newest refresh token (re-paste auth.json), the credential broker does not cover the blob, and read-only share-link runs strip it (`READ_ONLY_AGENT_ENV_KEYS` excludes it on purpose). Being an `agent-core/` change it needs BOTH agent images rebuilt. Tests: `tests/codex-chatgpt-auth.test.ts`.
- **Codex runs on the `codex app-server` protocol** (`agent-core/codex-app-server.ts` + `agent-core/codex-agent.ts`) — there is exactly ONE Codex execution path, no flag. The public `@openai/codex-sdk` is a wrapper over `codex exec --experimental-json` (one process per turn, child's stdin closed before the turn starts), so a Slack follow-up on a Codex run could only ever become a queued second run (the ⏳ reaction); that path is deleted. We keep the npm dependency ONLY because it vendors the `codex` binary that `resolveCodexBinary` locates via its platform packages (`CODEX_APP_SERVER_BIN` overrides it, and is the seam the tests point at a fake server). `codex app-server` is the persistent JSON-RPC-over-stdio protocol the real Codex app speaks; we drive `initialize`/`initialized` → `thread/start` (or `thread/resume`) → `turn/start` (carrying `CODEX_SUBMISSION_JSON_SCHEMA` as `outputSchema`) and pump `options.inputChannel` into **`turn/steer`**, exact parity with the Claude side's `AgentInputChannel` — so both harnesses are steerable and both runners register an injector unconditionally. The protocol is EXPERIMENTAL and its v2 `ThreadItem`s are camelCase (`commandExecution`, `agentMessage`, `reasoning`, `mcpToolCall`), mapped onto the existing timeline strings by `codexV2ItemProgress`; `turn/plan/updated` is re-emitted as the existing `TodoWrite:` snapshot so the plan rail needs no client change. Invariants: register the notification handler BEFORE sending `turn/start` (notifications can precede the response); every steer carries `expectedTurnId`, and a rejected steer becomes a visible `error` timeline event rather than a silent drop; the client answers every server→client request (`-32601`) so the server can never hang; `resolveCodexBinary` is anchored on `__dirname` AND `process.argv[1]`, never only `process.cwd()` (the container runs with cwd `/workspace` while node_modules is at `/agent/node_modules`); the argv[1] anchor is mandatory because the agent images run agent-core via tsx under `"type": "module"` where `__dirname` does not exist — losing it broke every containerized Codex run (2026-08-16 scheduled-task failures) while host CJS tests kept passing, which is why `tests/codex-app-server.test.ts` has a child-process ESM-layout repro and must not resolve `@openai/codex-sdk/package.json` (not in its `exports`). `lib/slack/events.ts` needs no changes: the inject→⏳→new-run ladder already keys off the injector, and the ⏳ queue now only fires for cross-process/backend cases. Being an `agent-core/` change it needs BOTH agent images rebuilt. Tests: `tests/codex-app-server.test.ts` (a fake app-server, `tests/fixtures/fake-codex-app-server.mjs`, holds the turn open until a steer arrives).
- **Submission correction loop**: application validation failures are model feedback, not immediate user-facing failures. Claude receives them as `submit_response` tool errors and may resubmit; Codex receives a same-thread correction prompt. Both get up to `MAX_SUBMISSION_ATTEMPTS` total submissions. Codex JSON parse failures use the same loop. Never fall back to trailing plain text after a rejected Claude submission; surface the final actionable rejection only after the bounded attempts are exhausted.
- **Live steering of a running run** (`agent-core/input-channel.ts`): the Claude harness runs in streaming-input mode, and `buildUserMessageStream(input, inputChannel)` keeps that iterator OPEN for the turn, so a message arriving mid-run is delivered as an extra user turn instead of becoming a queued follow-up. The channel is closed on `submit_response` capture, on the `result` frame (only when `pendingCount() === 0`, so a message landing exactly at the boundary still gets a turn), and again in the run's `finally` — leave all three, or the query never ends. Host lookup is the process-local `registerRunMessageInjector`/`injectRunMessage` in `lib/agent-runner/run-registry.ts` (same scope as the abort controllers): in-process registers the channel directly; the container backend registers a writer that emits `{type:"user_message",text}` NDJSON frames on the child's stdin, which is therefore deliberately left open (`runner/agent-entrypoint.ts` reads the first stdin line as the job and every later line as a steering frame, and destroys stdin in its `finally` so the container can exit). `injectRunMessage` returning false is normal and must ALWAYS fall back to the existing ⏳ queue — unknown/other-process run, `http`/`selfHosted` backend, or a turn that already ended (both harnesses now deliver into the live turn — Claude via streaming input, Codex via the app-server's `turn/steer`). Slack: `lib/slack/events.ts` injects with `buildSteeringMessage` + 👀 and records the anchor in `steeredRunAnchors` so `onFinished` reacts on it too. **The stdin protocol is not backward compatible — rebuild both agent images (below) or container runs hang.** Tests: `tests/agent-input-channel.test.ts`, `tests/slack-bot.test.ts`.
- **The run registry must live on `globalThis`** (`lib/agent-runner/run-registry.ts`): Next evaluates `instrumentation.ts` (Slack socket + scheduler, owner of every Slack-triggered run) in a DIFFERENT module context than App Router route handlers, so module-local Maps gave each context its own registry — a Slack run was uncancellable from the UI (409 "not owned by the current server process"), `/api/health` reported `activeRuns: 0`, and the blue/green drain therefore exited after its grace period and killed in-flight Slack runs. Both maps hang off `RUN_REGISTRY_GLOBAL_KEY`. Test: `tests/agent-cancel.test.ts`. The same rule applies to Slack's mid-run thread bookkeeping (`lib/slack/thread-state.ts`, `SLACK_THREAD_STATE_GLOBAL_KEY`): `queuedFollowUps` + `steeredRunAnchors` were module-local, so a follow-up steered from the socket context into a run started by the agent's `message_thread` route kept its 👀 forever (`onFinished` read an empty steered list). Any new cross-context in-memory map goes on `globalThis` too. Test: `tests/slack-bot.test.ts`.
- **One active agent session per Slack thread**: a Slack message or a firing `ScheduledTask` that lands on a thread with a live run is STEERING for that run (`steerActiveThreadRun` + `buildSteeringMessage`, 👀), never a second run — stacked runs all park on the per-conversation session lock, look hung, and never reply. Injection failure still falls back to the ⏳ queue. Related: conversation runs pass `deferHeartbeat: true` and call `ctx.beginHeartbeat()` only *inside* `withConversationLock` (`createDeferredHeartbeat` in `lib/ai-runs.ts`), so a run parked on the lock goes silent and the silence reaper clears it instead of it posing as a live-but-unsteerable run; that is safe because legitimate queued follow-ups are chained from `onFinished`, not parked on the lock. Tests: `tests/ai-run-reaper.test.ts`, `tests/slack-bot.test.ts`, `tests/scheduler.test.ts`.
- **`check_back_later` is the agent's alarm clock** (`lib/slack/agent-tools.ts`): a one-shot thread-anchored `ScheduledTask` (`after_minutes` 1–1440 + a self-contained `instruction`) with NO "⏰ Scheduled task created" announcement, whose result text orders the agent to end its turn. It replaces sleep/poll babysitting of long jobs: launch detached → set the alarm → end the turn → get woken in the same thread (and, per the rule above, injected into a live session if one exists). A new Slack agent tool must be registered in FOUR places or it silently 400s/goes missing: the `SlackAgentToolRequest` union + handler (`lib/slack/agent-tools.ts`), the zod enum in `app/api/slack/agent-tools/route.ts`, the Codex MCP schema list (`lib/slack/agent-tools-mcp.ts`), and the Claude side in `agent-core/agent.ts` (tool def + the tools array + `SCHEDULE_TOOL_NAMES` allowlist + the Slack system-prompt block). Tests: `tests/slack-agent-tools.test.ts`, `tests/slack-agent-tools-mcp.test.ts`.
- **Keep-alive parking while a wake-up is pending** (`agent-core/turn-park.ts`): "detach and die" is a lie in a container run — `docker run --rm` PID 1 exits at run end, taking the nohup'd child, its logs and the per-run worktree with it. So a successful `check_back_later` within `MAX_KEEP_ALIVE_MINUTES` (120) ARMS a park: the harness does NOT close the steering channel at the turn's `result` frame, the SDK session idles at zero token cost, the container/worktree/background processes survive, and the scheduler's firing is injected into that same live session (👀 persists, so ✅ finally means "everything done"). Delivery disarms the park; a `parkTimer` deadline of delay + `PARK_GRACE_MS` (5 min) pushes `PARK_TIMEOUT_NUDGE` so a lost beat ends in a real submission instead of an idle hang; longer delays refuse to park and get the old fresh-run semantics, which the tool result states explicitly (KEEP-ALIVE ON/OFF). While parked, `submit_response` is rejected once (it would kill the background work) — status goes out via `post_slack_message` and the agent just ends the TURN, not the RUN. Consequences to respect: the park holds the per-conversation session lock and pins a blue/green drain for its duration (bounded by the 120-min cap), and the run's heartbeat must keep ticking, so never route a parked run through the deferred-heartbeat path. Tests: `tests/turn-park.test.ts`.
- **Keep-alive is STATE, the alarm is an EVENT** (`keep_alive_after_turn`, `agent-core/turn-park.ts` + `agent-core/background-work.ts`): the alarm park alone had a fatal hole — a user message delivered mid-park DISARMS it, and if the agent answers without re-arming, the reply turn's result frame ends the run and kills its own background job (2026-08-10 incident: the doc-links sweep died to a "How is it going?"). Both harnesses now enforce the same lifecycle: Claude owns `keep_alive_after_turn` as an in-process SDK tool; Codex receives it over the Slack MCP bridge and `codex-agent.ts` observes the completed MCP call before deciding whether the structured turn output is terminal. A successful short `check_back_later` or enabled keep-alive makes Codex ignore the nominal final JSON, retain the same app-server/container/worktree, and wait for the next injected turn; this closes the 2026-08-26 failure where Codex posted “Done”, destroyed `/tmp`, and woke in a fresh run. Message delivery clears only the alarm park; the keep-alive flag survives until explicit `enabled=false`, the absolute TTL (`KEEP_ALIVE_MAX_TOTAL_MS`, 24h), or final cleanup. While on, result frames re-arm a 30-minute check-in so a forgotten daemon cannot pin a container forever. Claude additionally detects pending background Bash work and asks the agent to choose a lifecycle; that detection is advisory only. Tests: `tests/turn-park.test.ts`, `tests/background-work.test.ts`, `tests/codex-app-server.test.ts`, `tests/slack-agent-tools-mcp.test.ts`.
- **A claimed scheduler beat is never dropped** (`deferBeat` in `lib/scheduler.ts`): the claim advances/disables the row BEFORE firing, so returning early when the thread has an active-but-unsteerable run (other process after a deploy, Codex, uncollected zombie) lost a `check_back_later` wake-up forever. Such a beat is now re-armed at `now + BEAT_DEFER_MS` (2 min, `disabledAt` cleared for one-shots) until it is more than `MAX_BEAT_DEFER_MS` (30 min) overdue, then it gives up with a `[scheduler]` error — the blocking run finishes or is reaped within `STALE_AI_RUN_MS`. Test: `tests/scheduler.test.ts` ("a beat that cannot be delivered is deferred, not lost").
- **Slack tool calls after submission are refused loudly, not silently** (`callSlackTool` in `agent-core/agent.ts`): a `check_back_later` issued in the same assistant turn as `submit_response` landed after the run was finalized and its broker keys revoked, so the route rejected it and the alarm silently never existed. Post-capture calls now return an actionable `isError` and emit a timeline `error` event; every Slack tool failure does the same.
- **Rebuild agent images after `agent-core/` changes**: `docker build -f runner/Dockerfile.agent -t gdocs-agent:local .` and `docker build -f runner/Dockerfile.codex-agent -t gdocs-codex-agent:local .` — deploy scripts do NOT do this. Override the Codex image with `CODEX_AGENT_CONTAINER_IMAGE`.
- **Detached session containers** (`agent-core/session-protocol.ts`, `agent-core/session-server.ts`, `lib/agent-runner/container-session.ts`, `lib/agent-runner/session-store.ts`; opt-in via `AGENT_DETACHED_CONTAINERS=1`, default off): instead of `docker run -i --rm` as a CHILD of Next.js joined by stdio pipes, the container is `docker run -d` with `-p 127.0.0.1::8787` and serves an HTTP session API (`/status`, `/attach`, `/job`, `/frames?since=&wait=` long-poll, `/message`, `/cancel`, `/release`). The container is the durable state holder — monotonic frame sequence numbers + ring buffer, and it holds the result until the host has persisted it and calls `/release` — so the app is a stateless reader whose only memory is a cursor. The ENTIRE handover surface between deployments is four `AiRun` columns: `containerId`, `sessionEndpoint`, `sessionSecret`, `frameCursor`; the handle is written BEFORE the job is posted (an unreachable running container is the failure mode this design removes) and the endpoint/secret are cleared on release. Two auth gates: loopback-only published port AND a per-container Bearer secret that rides the `--env-file`, never the docker argv (`ps` is world-readable). Single-attach arbitration lives in the container (`/attach` invalidates the previous token; a superseded reader gets `409 attach-superseded` and must stop polling — never retry, competing readers double-persist frames). Cancellation is in-container (`/cancel` → in-process `AbortController`); a detached container has no parent to signal, so never `docker rm -f` a live one. `runner/agent-entrypoint.ts` picks the transport on `AGENT_SESSION_PORT` being set and keeps the legacy stdio mode otherwise — **an image built before this change hangs under `-d`** (it only reads stdin and never gets a job line), so rebuild both agent images before flipping the flag. The reaper is detached-aware (`failAbandonedAiRuns`): a silent run whose `sessionEndpoint` still answers is spared, but sparing is re-earned every sweep and deliberately does NOT fake a heartbeat, and the probe uses `/status?probe=1` which does NOT count as contact — otherwise the check meant to notice that nobody is driving an orphan would keep it alive past its own no-contact TTL. Tests: `tests/agent-session-protocol.test.ts`, `tests/agent-session-transport.test.ts`, `tests/agent-session-host.test.ts`, `tests/agent-session-store.test.ts`, `tests/agent-session-reaper.test.ts`, `tests/agent-session-adoption.test.ts`, `tests/agent-container.test.ts`. **Boot adoption** closes the loop for CONVERSATION-family runs (`CONVERSATION`/`CONVERSATION_FOLLOWUP`/`SLACK_MENTION`/`SLACK_FOLLOWUP`): `adoptOrphanedSessions` (`lib/agent-runner/session-adoption.ts`) runs from `instrumentation.ts` BEFORE the silence sweep (gated on `detachedContainersEnabled`, non-blocking, `[agent-session]`-prefixed logs), finds RUNNING/PENDING runs that still advertise `sessionEndpoint`+`sessionSecret`, probes each, LEAVES unreachable ones to the reaper (adoption never reaps), and for the rest re-attaches with **no job** (never a second agent turn), resumes frames from the persisted `frameCursor` so nothing at/below it is re-emitted, restarts the heartbeat, re-registers the abort controller + steering injector, records a visible "adopted by a new server process" timeline event (never adopt silently), then finalizes through the shared `finalizeConversationRun` (`lib/agent-conversation.ts` — extracted from the live tail so both paths emit identical events/fields) and `/release`s the container. Repeat calls are safe via a `globalThis` in-flight set (same pattern as the run registry). Deliberate limits: the adopted path does NOT reconstruct a commit (it records a timeline event naming the worktree where changes were left uncommitted rather than inventing a half-working commit), and non-conversation trigger types are left RUNNING for the reaper with a `[agent-session]` warning naming the run id and trigger type. Slack reply delivery uses only persisted state (`AiRun.triggerId` = `<channel>:<threadTs>`); the 👀→✅ reaction swap, `steeredRunAnchors` and `queuedFollowUps` live only in memory in `lib/slack/events.ts`, so an adopted Slack run replies but does not update the reaction — persisting the trigger message `ts` would close that gap. Still open: not pinning the blue/green drain, and raising `MAX_KEEP_ALIVE_MINUTES`. **Adoption must never take a run whose reader is still alive** (2026-08-11 incident, first real use of detached containers): a blue/green boot adopted a Slack run the DRAINING sibling was actively driving — a reachable container proves nothing about its reader, and the most reachable container is one whose owner is working in it. Blue's `/attach` invalidated green's token → green's frames poll got 409 `AttachSupersededError` → the generic lifecycle catch marked the run FAILED → Slack posted "The run failed: Another process attached to this agent session container." → the credential broker revoked the still-working agent's virtual keys, so it died mid-sweep with a 401, and container cleanup then removed the terminal run's container (why the host showed none). Two guards, both mandatory: (1) `hasLiveOwner` in `session-adoption.ts` skips (reason `"live-owner"`) any candidate this process still holds an abort controller for (`isCancellableAiRun`) or whose `heartbeatAt`/`startedAt` is younger than `LIVE_OWNER_SILENCE_MS` (3 × `AI_RUN_HEARTBEAT_INTERVAL_MS` = 3 min, well inside `STALE_AI_RUN_MS`) — so boot adoption alone is no longer sufficient and `instrumentation.ts` runs the same sweep on every 5-min reaper tick (adoption first, silence sweep second). (2) A superseded reader treats `AttachSupersededError` as a HANDOVER, not a failure: `withAgentRunLifecycle` returns `{status:"HANDED_OFF"}`, records only a system event (`SESSION_HANDED_OFF_MESSAGE`), skips the FAILED write, the error event, the failure-path commit AND the worktree removal (the other reader's container is still writing that bind mount), and `runConversationInBackground` returns without calling `onFinished` (no bogus Slack failure post; the adopting process delivers the reply). Tests: `tests/agent-session-adoption.test.ts` ("a run whose owner is still heartbeating is not stolen from it"), `tests/agent-run-handover.test.ts`.
- **Agent container resource ceilings** (`container-args.ts` / `container.ts`): `--memory` (`AGENT_CONTAINER_MEMORY`, default 4g), `--cpus` (`AGENT_CONTAINER_CPUS`), and `--pids-limit` (`AGENT_CONTAINER_PIDS_LIMIT` via `resolveContainerPidsLimit`, default 512, floor 64, `-1` = unlimited). The pids cap is invisible to the agent and it CANNOT raise it from inside (read-only cgroupfs + `cap-drop ALL`), so fan-out workloads (ML training with dataloader workers, parallel sweeps) just see children die with rc=1 and misdiagnose it as an app bug. Raise the env var host-side when a channel does that kind of work. Test: `tests/agent-container.test.ts`.
- **Docker-in-agent-container via Sysbox is the DEFAULT, detected — not flagged** (`container-args.ts` / `container.ts` / `runner/agent-entrypoint.ts`): when the host Docker engine has the `sysbox-runc` runtime registered (one-time host install of sysbox-ce; probed once per process via `docker info --format '{{json .Runtimes}}'`, cached — `detectSysboxRuntime` / pure parser `sysboxAvailableFromRuntimes`), EVERY agent container runs as a Sysbox "system container": `--runtime sysbox-runc`, and the runc-profile flags are intentionally ABSENT because they would break the inner dockerd — no `--user` (dockerd needs container root; the entrypoint's `maybeStartInnerDockerd` starts it, gated on the runner-injected `AGENT_INNER_DOCKER=1` marker + uid==0 + `/usr/bin/dockerd` exists + no socket, non-fatal on failure — root alone is NOT the signal, Docker Desktop's hardened profile is also root and probing there would waste 20s/run), no `--cap-drop ALL`/`no-new-privileges` (userns root needs in-namespace caps), no `--read-only`+tmpfs (sysbox mounts writable dirs over `/var/lib/docker`). Isolation comes from the user namespace + virtualized `/proc`/`/sys` instead: the agent gets its OWN empty docker daemon and can never see host containers (the host socket is still never mounted). Resource ceilings, network, env-file, mounts, and `IS_SANDBOX=1` are unchanged. No sysbox on the engine → the hardened runc profile applies exactly as before. Kill-switch/override: `AGENT_CONTAINER_OCI_RUNTIME` (e.g. `runc` forces the hardened profile, `runsc` = gVisor) — an explicit value always wins and disables the sysbox default; there is deliberately no separate feature flag. Both agent images bake docker-ce + `/etc/docker/daemon.json` with inner address pool `10.88.0.0/16` (never collides with the outer `172.17.0.0/16` bridge). After installing sysbox on a host, run `runner/verify-sysbox-docker.sh` — it verifies the bind-mount ownership round-trip (container-root writes must come back host-user-owned, or the app can't commit agent output) and inner-daemon isolation, BEFORE restarting the service. Being partly an entrypoint/image change it needs both agent images rebuilt. Tests: `tests/agent-container.test.ts` (sysbox profile + parser).
- **Container UID on Docker Desktop**: macOS bind mounts appear as `root:root` inside Docker Desktop's Linux VM, so passing the macOS numeric UID makes `/workspace` and the mounted SDK session directory unwritable (`Codex app-server ... Permission denied`). `resolveContainerUser` deliberately omits `--user` on Darwin; the container remains restricted by dropped capabilities, `no-new-privileges`, read-only rootfs, and scoped mounts, while Docker Desktop maps bind writes back to the macOS owner. Claude runs additionally receive `IS_SANDBOX=1`, Claude Code's supported marker for permitting `bypassPermissions` as root when an outer sandbox is the security boundary; without it Claude exits before starting. That marker must survive BOTH environment boundaries: Docker args in `container-args.ts` and the SDK subprocess allowlist in `agent-core/agent-env.ts`. Codex must not receive this Claude-specific marker. Native Linux keeps `--user <host uid>:<host gid>`. Regressions: `tests/agent-container.test.ts`, `tests/agent-env.test.ts`.

- **Settings screens** (`/settings/<section>`, topbar button "Settings"): `SettingsNav` (`components/settings-nav.tsx`) tabs across sections — `/settings/agent` (AI & credentials), `/settings/forum` (default quicktake audience), and `/settings/notifications` (Slack DM controls plus the default state for document comment notifications). Per-document comment notifications are toggled with the bell in each document's topbar; an explicit `DocumentNotificationPreference` overrides the user default. Notification dispatch lives in `lib/activity-notifications.ts` and `lib/comment-notifications.ts`; new notification types must remain opt-in and fire-and-forget. `/settings` redirects to the first section. Sharing a document with a group requires only *membership* of that group, while group member management stays owner-only.

## Things that have caused real failures (so far)

- **`next build` OOM (2026-08-18).** The Next.js build worker died with `FATAL ERROR: Reached heap limit Allocation failed` (SIGABRT) under the default node heap, failing a deploy. Retry with `NODE_OPTIONS="--max-old-space-size=8192" ./deploy/deploy.sh` worked. The failed build leaves the old color serving — the site stays up; just rerun with the bigger heap (consider baking this into `deploy/deploy.sh`).

- **Cloudflare 524 on long edits (FIXED — historical).** Server finished, client never got the response (`status:524` + `elapsedMs ≥ ~100000`). Fixed by making AI edits async: `POST /ai-edit` creates the `AiRun`, fires `runAiEditInBackground`, and returns immediately; the client polls run status and fetches the result (`ai-edit-kickoff` / `ai-edit-fetch-result` scopes). The ~100s Cloudflare ceiling now only matters for any NEW synchronous endpoint that does slow work inline — don't add one.
- **Selection marker lost during long agent runs.** Any doc mutation while the agent is working can strip the `aiEditRange` mark. Diagnosed via `scope:ai-edit-marker-lost`'s `presence` payload.
- **Iframe self-feedback in widget views.** Plotly-style autosize widgets used to inflate to 6× viewport height. `EmbeddedWidgetView` in `nodes.tsx` now ignores `ResizeObserver` ticks that look like echoes of its own height changes.
- **Comments on atom block nodes** (widget / repoImage / image). Originally impossible because `commentAnchor` is an inline Mark. Now stored as `commentThreadIds` attribute on the block node.
- **Ghost agent containers + leaked test scheduled tasks (2026-07-28).** `tests/scheduler.test.ts` runs against the REAL DB; a recurring fixture task left enabled fired daily via the production scheduler (36 leaked "check the eval dashboard" crons → 24 real agent runs/containers at 09:00). Separately, `docker run --rm` agent containers outlive a dying server process (deploy, crash, docker restart) and keep burning tokens with nobody consuming the result. Fixes: scheduler tests must disable the tasks they create; the reaper now `docker rm -f`s reaped runs' containers, and `reconcileRunContainers` (`lib/agent-runner/container-cleanup.ts`) sweeps containers of terminal/unknown runs on boot + every reaper pass, sparing RUNNING/PENDING (a draining blue/green sibling's runs). Tests: `tests/container-cleanup.test.ts`. Rule: any test that writes rows a background loop polls (scheduler, reaper) must leave them disabled/terminal.
- **Tab key stole focus into the tab-title rename input (2026-07-28).** Any focusable element rendered inside the editor DOM (the tab-title `<input>` + copy button in `TabBreakView`) becomes a browser Tab-navigation target whenever ProseMirror doesn't consume a Tab/Shift-Tab press (unsinkable first list item, plain paragraph, pre-hydration keypress) — the caret "jumps to the tab heading" instead of indenting. Fixes: `tabIndex={-1}` on both controls, plus the low-priority `TabIndentGuard` extension (`editor-extras.ts`) that swallows unhandled Tab/Shift-Tab after ListItem/TaskItem/Table bindings run. Rule: any focusable control inside a node view needs `tabIndex={-1}`. Test: `e2e/tab-key-indent.spec.ts` (note: e2e keypresses right after page load race hydration — click, assert `toBeFocused`, settle ~300ms first).
- **"Error fetch failed" on image-heavy documents (2026-08-12).** Two selection edits on a document with 36 pasted screenshots failed after ~13 s with the opaque message `fetch failed`, retried 3× by the transient-retry path, `frameCursor 0`. Cause: the job payload carries `documentBlocks` — pasted images as base64 data URLs — so `POST /job` was ~6 MB, over the 4 MiB `MAX_BODY_BYTES` in `agent-core/session-server.ts`, whose overflow path `req.destroy()`ed the socket mid-upload; undici reports a destroyed socket as the bare `fetch failed`, so nothing in the log named the real limit. Fixes: the ceiling is now `AGENT_SESSION_MAX_BODY_BYTES` (default 128 MiB, floor 4 MiB, `MAX_SESSION_BODY_BYTES`); an over-limit body is DRAINED and answered with a real `413` instead of a socket destroy; `session-client.ts` names the method+endpoint on transport errors and includes the server's error body in `SessionGoneError`. Note `413` is deliberately not in `isRetryableAgentError`'s pattern, so a genuine overflow fails once instead of 3×. Rule: never reject an HTTP body by destroying the request — the caller loses the status and the reason. Tests: `tests/agent-session-transport.test.ts` ("a multi-megabyte job…", "an over-limit job body fails with an actionable HTTP error…"). Being an `agent-core/` change, it needs both agent images rebuilt.
- **Orphaned next-server serving a stale build.** Killing the `.service.pid` process leaves the `next-server` child alive; it survives SIGTERM, keeps serving the old build over cloudflared's established keep-alive connections even after losing its listener, and the next deploy's `npm run build` rewrites `.next` under it (old HTML → missing chunks → `ChunkLoadError: Loading chunk N failed`) before dying with EADDRINUSE. Diagnose with `ps aux | grep next-server` (more than one = bug) and per-process `lsof` on the log fds; fix is in `legacy-single-process.sh` (frees the port + kills established-connection holders) plus `ChunkReloadRecovery` client-side; the blue/green deploy avoids the problem entirely (each color has its own dist dir).
