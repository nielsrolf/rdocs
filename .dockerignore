# Keep the agent image build context small. The image only needs agent-core/ and
# runner/ (see runner/Dockerfile.agent); everything else is excluded.
node_modules
**/node_modules
.next
.git
.research-workspaces
logs
*.log
.env
.env.*
*.db
*.db-journal
prisma/*.db
**/*.db
**/*.db-journal
backups
playwright-report
test-results
e2e
tests
.claude
.service.pid
